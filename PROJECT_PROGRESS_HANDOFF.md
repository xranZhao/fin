# 家庭月度驾驶舱项目进展与 Claude Code 交接

> 交接日期：2026-08-11  
> 仓库路径：`D:\CLAUDE\finance-systerm\PWA系统`  
> 当前分支：`main`  
> 当前阶段：本地 V1 可用，等待按 V2 设计重构移动端 UI 与设置数据模型。

## 1. 一句话状态

现有项目已经完成可运行的本地 PWA、钱迹 CSV 汇总、月度快照、周期总结、生命能量基础计算、备份和只读预览；下一步不是推倒重写，而是在保留数据和计算能力的前提下，落实新的六页移动端 UI、双人横向对照和生命能量明细设置。

## 2. 已完成内容

### 2.1 基础应用

- 原生 HTML、CSS、JavaScript ES Module，无前端框架。
- Node 静态服务器用于本地开发。
- PWA manifest、图标和 service worker 已存在。
- 支持 `?demo=1` 演示数据；去掉参数进入真实空数据。
- 数据默认保存在当前浏览器 `localStorage`。

### 2.2 月度数据

- 可录入自然月、两张家庭卡余额、双人工资、家庭转入和个人留存。
- 同一月份作为唯一快照保存。
- 已有本地数据读写抽象，预留远程适配位置。
- 支持 JSON 导出和恢复。

### 2.3 钱迹 CSV

- 已实现浏览器端解析和月度汇总。
- 支持真实钱迹导出的编码与表头。
- 排除收入、退款、不计收支和非目标月份。
- 只保存总额、笔数、分类合计及文件追踪信息，不保存原始交易行。

### 2.4 分析与周期

- 已有总览、月度记录和生命能量三个主导航页面。
- 已支持按月查看。
- 已支持从月度快照自动生成半年总结和年终总结。
- 已有实际时薪、每赚 10 元所需时间及家庭生命时间基础计算。
- 已有储蓄、收入、共同转入、支出等趋势展示。

### 2.5 权限原型

- 已有管理视图和只读预览切换。
- 当前只用于本地交互验收，不是真实安全权限。
- 阿里云 API 契约和部署准备文档已经存在。

## 3. 当前验证结果

2026-08-11 已执行：

```powershell
npm run check
npm run test:unit
npm run test:smoke
```

结果：

- JavaScript 语法检查通过。
- 钱迹 CSV 与周期汇总单元测试通过。
- 真实浏览器核心流程冒烟测试通过。

## 4. 当前提交记录

```text
68e072a 优化周期总结与手机端生命能量页面
675966d 完成家庭财务PWA本地版与项目规格
```

当前 Git 仓库没有配置远程地址，因此可以本地提交，但不能执行 `push`。

## 5. 代码结构

| 文件 | 当前职责 |
|---|---|
| `index.html` | 三个主页面、设置和表单的 HTML 结构 |
| `styles.css` | 手机布局、组件、主题和响应式样式 |
| `app.js` | 页面状态、渲染、表单、计算和事件绑定 |
| `storage.js` | `localStorage` 状态、默认数据和远程适配抽象 |
| `csv-parser.js` | 钱迹 CSV 解码、字段识别和月度汇总 |
| `period-summary.js` | 月度、半年和年度周期计算 |
| `sw.js` | PWA 缓存 |
| `manifest.webmanifest` | PWA 安装信息 |
| `server.mjs` | 本地静态服务器 |
| `tests/unit.mjs` | CSV 与周期规则单元测试 |
| `tests/smoke.mjs` | 浏览器核心流程测试 |
| `docs/API_CONTRACT.md` | 未来云端 API 契约 |
| `docs/ALIYUN_DEPLOYMENT.md` | 阿里云部署准备 |

## 6. 最新确认的 V2 设计方向

六张 UI 预览图已生成，仅作为视觉与布局目标，尚未写入前端代码：

| 页面 | 预览图路径 |
|---|---|
| 总览 | `C:\Users\sageh\.codex\generated_images\019fd693-f8f1-7850-a2e0-a72a13d303a7\exec-f10d94f3-ca41-4553-ac78-98969d1e8f99.png` |
| 月度记录 | `C:\Users\sageh\.codex\generated_images\019fd693-f8f1-7850-a2e0-a72a13d303a7\exec-7afc1e44-ce34-445b-9588-2c4d50290028.png` |
| 生命能量 | `C:\Users\sageh\.codex\generated_images\019fd693-f8f1-7850-a2e0-a72a13d303a7\exec-5b574384-ecfd-42f8-a33c-7d7283cdb73b.png` |
| 支出分析 | `C:\Users\sageh\.codex\generated_images\019fd693-f8f1-7850-a2e0-a72a13d303a7\exec-54ed89bd-4885-4db8-bfbe-da3f037c7378.png` |
| 半年／年度总结 | `C:\Users\sageh\.codex\generated_images\019fd693-f8f1-7850-a2e0-a72a13d303a7\exec-5bbaf041-faf0-492c-bf56-1f3507baa03f.png` |
| 设置 | `C:\Users\sageh\.codex\generated_images\019fd693-f8f1-7850-a2e0-a72a13d303a7\exec-91a6629b-e5d2-45e7-aff0-3be4fd4d1b77.png` |

设计硬约束：

- 双人信息必须改成“项目名 + 酥梨 + 陈前”的横向矩阵。
- 工资、转入家庭、个人留存、实际时薪及设置资料都不得再上下堆叠两套人物卡。
- 390 像素宽手机应能一次看到两个人的对应数据。
- 月份选择升级为月度、半年度、年度三个口径。
- 设置页集中维护工资、工作天数、工作时长、通勤时间、工作餐和其他工作成本。
- 月度工资可覆盖设置中的参考月薪。

## 7. 尚未实现或需要重构

### P0：数据模型迁移

- 当前 `WorkProfile` 只有 `monthlyIncome`、`workHours`、`commuteHours`、`workCosts` 四个汇总字段。
- V2 要拆分为工作天数、每日工时、每日通勤分钟、工作餐、通勤成本和其他工作成本。
- 必须增加 `schemaVersion` 迁移并兼容现有本地数据。
- 在用户补齐 V2 设置前，旧计算结果不能突然失效。

### P1：全局手机布局

- 重构双人信息为统一三列矩阵。
- 检查 390×844 下无横向滚动、无遮挡、无过小文字。
- 保留现有三个底部导航入口，设置保留右上角入口。
- 清理现有页面中重复、上下堆叠和占高过大的组件。

### P2：六个页面落实

- 总览：家庭储蓄、预算、双人分配和关键洞察同屏。
- 月度记录：双人紧凑输入矩阵及钱迹上传结果。
- 生命能量：双人实际时薪对照及分类生命时间。
- 支出分析：分类金额、占比、生命时间和关键花费。
- 周期总结：月度／半年度／年度选择、双人贡献、目标和趋势。
- 设置：双人长期资料矩阵、计算预览和备份入口。

### P3：测试补充

- V1 → V2 设置迁移测试。
- 当月工资覆盖参考工资测试。
- 生命能量明细公式测试。
- 半年和年度不完整数据覆盖率测试。
- 390 像素视口双人矩阵与底部导航浏览器测试。
- 只读模式下所有新增编辑入口禁用测试。

### P4：云端同步，后续阶段

- 当前尚未配置阿里云函数计算、私有 OSS、域名和 HTTPS。
- 尚未实现真实管理密码、只读密码和服务端会话。
- 用户现在不需要提供阿里云账号或密钥。
- 本地 UI 与数据迁移稳定后再进入部署阶段。

## 8. 推荐实施顺序

1. 阅读 `PROJECT_REQUIREMENTS_V2.md`，以其作为 V2 单一目标来源。
2. 给 `storage.js` 增加 V2 工作资料结构和幂等迁移。
3. 把生命能量计算提取为可独立测试的纯函数。
4. 先实现通用“双人对照矩阵”样式，再替换总览、录入、生命能量和设置中的重复布局。
5. 落实设置页和计算预览。
6. 落实总览、月度记录、生命能量、支出分析和周期总结。
7. 补齐单元与浏览器测试。
8. 在本地演示数据和真实空数据两种模式下验收。
9. 本地版稳定后，再按 `docs/API_CONTRACT.md` 进入阿里云同步。

## 9. 开发约束

- 不安装新依赖，除非用户明确同意。
- 优先延续当前原生 HTML/CSS/JavaScript 技术栈，不因 UI 重构引入框架。
- 不硬编码设计稿中的金额和工作资料；演示值统一来自 demo 数据。
- 不删除或覆盖用户已有 `localStorage` 数据。
- 不改变钱迹 CSV 的隐私边界，不保存原始交易行。
- 不把本地只读预览伪装成安全权限。
- 所有新增代码注释使用中文。
- 修改后至少执行 `npm run check`、`npm run test:unit` 和 `npm run test:smoke`。

## 10. 启动方式

```powershell
cd "D:\CLAUDE\finance-systerm\PWA系统"
npm start
```

演示地址：

```text
http://127.0.0.1:4173/?demo=1
```

真实空数据：

```text
http://127.0.0.1:4173/
```

## 11. Claude Code 完成定义

一次 V2 UI 与设置重构只有同时满足以下条件才算完成：

- 六类页面按需求实现，而非只修改首页颜色。
- 双人同类数据全部横向同行展示。
- 设置明细可以保存、恢复，并正确驱动生命能量计算。
- V1 本地数据可无损迁移。
- 月度、半年、年度汇总结果正确。
- 管理和只读视图都通过浏览器验收。
- 三条现有检查命令全部通过。
- 在 390×844 视口截图核对无横向滚动和底栏遮挡。

## 12. 当前不需要用户提供的内容

- 不需要阿里云账号密码。
- 不需要 AccessKey。
- 不需要重新上传历史账单。
- 不需要立刻填写真实工资、通勤和工作成本。

真实默认资料应在设置页开发完成后，由用户在手机端自行填写。

