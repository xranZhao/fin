# 家庭月度驾驶舱 V2 项目需求文档

> 文档状态：可直接交接开发  
> 更新日期：2026-08-11  
> 产品负责人：酥梨  
> 本文档是 V2 目标规格；与旧文档冲突时，以本文档为准。

## 1. 项目背景

家庭已经使用钱迹记录逐笔收支，但钱迹解决的是“账目明细”，不能快速回答家庭层面的几个问题：本月赚了多少、共同留下多少、花了多少、离储蓄目标还有多远，以及这些消费换走了多少生命时间。

本项目是一个可安装到手机桌面的家庭财务 PWA。它不重复做记账，而是在每月一次的复盘中记录家庭大数，读取钱迹月度 CSV 的汇总结果，并生成月度、半年度和年度视图。

产品理念来自：

- 《自动百万富翁》：工资到账后先自动储蓄，再安排消费；优先检查“是否先付给家庭”。
- 《要钱还是要生活》：用实际时薪把消费换算成生命时间，讨论“值不值得”，而不是制造消费羞耻。

## 2. 产品目标

### 2.1 核心目标

夫妻两人每月用不超过 10 分钟完成一次家庭财务复盘，并能清楚回答：

1. 两人本月分别收入多少？
2. 两人分别转入家庭多少、个人留存多少？
3. 家庭本月总支出多少，是否超过 3,000 元参考预算？
4. 家庭花销卡和家庭储蓄卡当前余额是多少？
5. 家庭储蓄卡本月净增长多少，目标进度如何？
6. 每赚 10 元需要付出多少时间？本月支出换走了多少生命时间？
7. 本月、半年和全年，钱主要去了哪里？

### 2.2 成功标准

连续三个月同时满足：

- 酥梨可以独立在手机端完成月度录入和钱迹 CSV 上传。
- 单次录入、确认和保存总耗时不超过 10 分钟。
- 陈前打开只读链接即可看到与管理端一致的最新数据。
- 双方能从首页直接说出家庭收入、共同转入、支出、储蓄变化和目标进度。
- 默认生命能量资料只维护一次，月度只在实际工资变化时覆盖。

## 3. 产品定位与边界

### 3.1 产品定位

- 这是“家庭月度驾驶舱”，不是逐笔记账软件。
- 钱迹继续负责逐笔交易、分类、备注和退款处理。
- 本系统只保存月度大数、分类合计及分析结果。
- 记录从正式启用月份开始，不要求补录 2016 年以来的全部历史交易。

### 3.2 V2 范围

- 手机优先、可安装的 PWA。
- 总览、月度记录、生命能量、支出分析、周期总结、设置六类界面。
- 钱迹月度 CSV 浏览器端解析。
- 本地数据保存、JSON 备份与恢复。
- 管理视图和只读视图。
- 月度、半年度、年度自动汇总。
- 双人和家庭维度的生命能量计算。
- 为阿里云同步预留接口，但 V2 UI 重构阶段不要求立即上线云端。

### 3.3 明确不做

- 不做逐笔记账、交易编辑或替代钱迹。
- 不抓取银行卡、支付宝或微信交易。
- 不保存原始钱迹 CSV、交易对手或交易备注到云端。
- 不管理房产、车辆、基金组合、负债和个人全部净资产。
- 不自动执行转账，也不负责生活协议的法律执行。
- 不开放公众注册，不做多家庭、多管理员。

## 4. 用户与权限

### 4.1 管理者：酥梨

可查看、录入、上传、修改、覆盖月份、调整设置、导入及导出备份。

### 4.2 查看者：陈前

看到与管理者一致的页面和数据，但所有输入、上传、保存、恢复和删除入口均不可用。

### 4.3 访问方式

- 本地阶段：管理视图和只读预览用于交互验证，不代表真实安全权限。
- 云端阶段：同一 HTTPS 地址，分别使用管理密码和只读密码登录。
- 服务端建立会话并判断权限；浏览器不得保存阿里云 AccessKey。
- 不在 URL 中明文携带访问密码。

## 5. 信息架构

底部固定三个一级入口：

1. `总览`
2. `月度记录`
3. `生命能量`

二级入口：

- `支出分析`：从总览或生命能量进入。
- `家庭财务总结`：从总览的周期选择进入。
- `设置`：从总览右上角进入。

## 6. 全局交互与视觉要求

### 6.1 双人信息规则

所有酥梨与陈前的同类信息必须采用横向对照矩阵：

- 左列为项目名。
- 中列为酥梨。
- 右列为陈前。
- 同一项目的两个值必须在同一行。
- 禁止把两个人做成上下堆叠的两套长卡片。
- 390 像素宽手机不允许横向滚动。

适用范围：本月分配、月度录入、生命能量、半年贡献、年度贡献、设置资料。

### 6.2 视觉方向

- 米白哑光背景、炭黑文字、森林绿主色、浅鼠尾草绿承载区。
- 暖珊瑚色仅用于重要提醒或变化，不作为大面积背景。
- 禁止渐变、玻璃拟态、悬浮头像和无意义图表。
- 金额使用等宽数字感，并保持小数和千分位一致。
- 正文在手机上保持清晰可读，不通过缩小字号强塞内容。
- 卡片圆角约 16px，主要通过分隔线和留白建立层级。
- 一屏优先回答问题，详情按需进入二级页面。

### 6.3 周期选择

统一提供三个口径：

- 月度
- 半年度：上半年或下半年
- 年度

半年度和年度数据全部由月度记录自动生成，不增加重复录入。数据不完整时必须显示“已记录 N/6 个月”或“已记录 N/12 个月”。

## 7. 页面功能规格

### 7.1 总览

目标：一屏回答“家庭现在怎么样”。

必须包含：

- 当前查看月份或周期。
- 家庭储蓄卡余额、本期变化、目标进度。
- 家庭总支出、月度预算、预算剩余、预算使用率。
- 双人本月分配矩阵：到账工资、转入家庭、个人留存、待说明差额。
- 家庭共同转入合计、家庭储蓄率。
- 最多三条中性建议。
- 进入支出分析、周期总结和设置的入口。

建议不得羞辱消费，也不得把“花得最少”当成唯一目标。

### 7.2 月度记录

目标：每月只录大数，六分钟左右完成。

录入内容：

- 自然月。
- 家庭花销卡余额。
- 家庭储蓄卡余额。
- 酥梨与陈前的到账工资、转入家庭、当月个人留存。
- 钱迹月度 CSV，或人工确认家庭总支出。
- 200 字以内的本月大事或异常说明。

交互要求：

- 两人的三个金额使用同一张横向输入矩阵。
- 上传后显示文件名、有效支出笔数、自动汇总额和前三分类。
- 自动汇总额可以被人工确认值覆盖，但两者都保留。
- 同一月份只有一条记录；覆盖前必须二次确认。
- 保存后进入该月份总览。
- 只读模式下所有编辑和上传入口禁用。

### 7.3 生命能量

目标：把金额换算成生命时间，帮助判断“是否值得”。

必须包含：

- 本月家庭支出换走的总小时数和约合工作日。
- 酥梨与陈前的实际时薪、每赚 10 元所需分钟，横向同屏比较。
- 家庭综合实际时薪。
- 一级分类的金额、占比和生命小时。
- 最大支出分类和一个可调整金额对应的可换回时间。
- 计算口径入口。

缺少有效工作资料时显示“待完善计算资料”，不显示伪造的 0 元时薪。

### 7.4 支出分析

目标：回答“这个月，钱去了哪里”。

必须包含：

- 家庭总支出、预算使用率、生命时间。
- `好好吃饭`、`生活成本`、`品质生活`三类的占比、金额和生命时间。
- 最多三项关键花费；是否能显示关键花费取决于钱迹导入是否保留对应汇总，不能凭空生成。
- “查看钱迹明细”只作为回到钱迹的提示，不在本系统保存或重建逐笔明细。
- 一条简短且中性的建议。

### 7.5 家庭财务总结

目标：支持月度、半年度和年度复盘。

必须包含：

- 月度／半年度／年度分段选择器。
- 当前年份及上半年／下半年选择器。
- 累计收入、家庭支出、共同转入、个人留存、生命时间。
- 期初到期末的家庭储蓄变化和储蓄率。
- 双人累计贡献横向矩阵。
- 家庭储蓄目标进度。
- 月度趋势及数据覆盖月份数。
- 一条基于事实的总结，不把不完整数据描述成完整半年或全年。

### 7.6 设置

目标：一次维护长期默认资料，避免每月重复填写。

家庭默认值：

- 月度花费预算，默认 3,000 元。
- 年度或阶段性家庭储蓄目标。
- 两人显示名称。

每个人的生命能量默认资料：

- 参考税后月薪。
- 每月工作天数。
- 每天工作小时。
- 每个工作日往返通勤分钟。
- 每个工作日工作餐成本。
- 每月通勤成本。
- 每月其他必要工作成本。

设置页要求：

- 两个人使用同一张横向设置矩阵。
- 当月到账工资优先于参考月薪参与当月计算；参考月薪用于无月度覆盖的预览。
- 实时预览两人的实际时薪和每赚 10 元所需分钟。
- 支持保存、导出 JSON 备份、从 JSON 恢复。
- “批量导入”只导入设置模板或完整业务备份，不导入逐笔消费。

## 8. 数据模型 V2

### 8.1 根对象

```text
HouseholdFinanceState
├─ schemaVersion
├─ settings
├─ snapshots[]
└─ metadata
```

`schemaVersion` 从现有 `1` 升级时必须提供无损迁移，不得清空用户已有月份。

### 8.2 月度记录 `MonthlySnapshot`

```text
month: YYYY-MM
accounts.familySpendingBalance: number
accounts.familySavingsBalance: number
people.suli.income: number
people.suli.householdTransfer: number
people.suli.privateKept: number
people.chenqian.income: number
people.chenqian.householdTransfer: number
people.chenqian.privateKept: number
expense.autoTotal?: number
expense.confirmedTotal: number
expense.recordCount?: integer
expense.categoryBreakdown?: Record<string, number>
expense.sourceFileName?: string
expense.sourceMonths?: YYYY-MM[]
expense.importedAt?: ISO datetime
note?: string
createdAt: ISO datetime
updatedAt: ISO datetime
```

### 8.3 设置 `HouseholdSettings`

```text
monthlyBudget: number
savingsGoal: number
theme: system | light | dark
role: manager | viewer
people.suli.name: string
people.suli.workProfile: WorkProfileV2
people.chenqian.name: string
people.chenqian.workProfile: WorkProfileV2
```

### 8.4 工作资料 `WorkProfileV2`

```text
referenceMonthlyIncome: number
workDaysPerMonth: number
workHoursPerDay: number
commuteMinutesPerDay: number
mealCostPerWorkday: number
commuteCostPerMonth: number
otherWorkCostPerMonth: number
```

### 8.5 V1 数据迁移

现有 V1 字段：

```text
monthlyIncome
workHours
commuteHours
workCosts
```

迁移原则：

- `monthlyIncome` 映射到 `referenceMonthlyIncome`。
- 无法可靠拆分的 `workHours`、`commuteHours`、`workCosts` 必须原样保留在兼容字段或迁移备注中。
- 在用户补齐 V2 明细前，计算继续使用 V1 汇总字段，避免计算结果突然变化。
- 用户保存新的 V2 设置后，再切换为 V2 明细公式。
- 迁移必须幂等，重复启动不能重复转换或损坏数据。

## 9. 计算口径

### 9.1 家庭资金

```text
家庭总收入 = 两人到账工资之和
家庭共同转入 = 两人转入家庭之和
家庭个人留存 = 两人当月个人留存之和
个人待说明差额 = 到账工资 - 转入家庭 - 当月个人留存
家庭待说明差额 = 两人待说明差额之和
家庭经营结余 = 家庭共同转入 - 家庭总支出
预算剩余 = 月度预算 - 家庭总支出
预算使用率 = 家庭总支出 / 月度预算
储蓄目标进度 = 家庭储蓄卡余额 / 储蓄目标
```

家庭储蓄率的展示口径必须在界面上说明。推荐使用：

```text
家庭储蓄率 = 家庭共同转入 / 家庭总收入
```

它表示“先付给家庭”的比例，不等同于净资产增长率。

### 9.2 生命能量

```text
月工作小时 = 每月工作天数 × 每天工作小时
月通勤小时 = 每月工作天数 × 每日通勤分钟 ÷ 60
月工作餐成本 = 每月工作天数 × 每日工作餐成本
月必要工作成本 = 月工作餐成本 + 每月通勤成本 + 每月其他工作成本
计算月薪 = 当月到账工资（存在且大于 0 时）否则参考税后月薪
个人实际时薪 = (计算月薪 - 月必要工作成本) / (月工作小时 + 月通勤小时)
每赚 10 元所需分钟 = 10 / 个人实际时薪 × 60
家庭综合实际时薪 = 两人扣除工作成本后的计算月薪之和 / 两人工作与通勤小时之和
家庭支出的生命小时 = 家庭总支出 / 家庭综合实际时薪
分类生命小时 = 分类金额 / 家庭综合实际时薪
```

异常处理：

- 时间分母小于等于 0 时不计算。
- 扣除工作成本后的收入小于等于 0 时不计算。
- 缺失资料显示待完善状态，不使用 0 或无穷大。

### 9.3 周期汇总

- 月度：单个月份快照。
- 半年度：1—6 月或 7—12 月的已有快照求和。
- 年度：1—12 月的已有快照求和。
- 平均月支出只除以实际记录月份数。
- 储蓄变化以周期内首末有效余额及周期前最近记录计算。
- 分类结构对周期内 `categoryBreakdown` 按一级分类求和。

## 10. 钱迹 CSV 导入规则

- 支持 UTF-8、UTF-8 BOM 和 GB18030。
- 识别时间、一级分类、二级分类、类型、金额及可选的不计收支字段。
- 只统计所选月份中 `类型=支出` 的正金额记录。
- 排除退款、不计收支、收入和其他月份。
- 页面展示有效笔数、自动总额和一级分类合计。
- 原始 CSV 只在浏览器内解析；本地状态和未来云端均不保存原始交易行。
- 同一文件重复上传不能产生重复月份记录；保存仍以月份唯一键覆盖。

## 11. 本地、备份与云端

### 11.1 本地阶段

- 使用 `localStorage` 保存业务状态。
- PWA 外壳可离线打开。
- JSON 导出包含设置和月度快照，不包含原始 CSV。
- JSON 恢复前校验结构和版本，并二次确认覆盖。

### 11.2 阿里云阶段

- 前端静态资源与 API 使用同一 HTTPS 域名。
- 函数计算提供登录、读取、保存和版本冲突处理。
- 私有 OSS 只保存汇总 JSON，不公开桶。
- 管理会话可读写，只读会话只能读取。
- 服务端持有云端密钥，浏览器不持有 AccessKey。
- 正式上线前才需要用户登录阿里云控制台配合配置，不需要在聊天中提供账号密码或密钥。

## 12. 隐私与安全

- 禁止保存银行卡号、身份证号、阿里云 AccessKey 和明文访问密码。
- 禁止把家庭财务数据提交到公开 Git 仓库。
- 只读模式必须由服务端权限保证，不能只依赖隐藏按钮。
- 云端保存对象应有版本号或 ETag，避免两个设备互相覆盖。
- 导出文件明确提示包含家庭财务隐私，由用户自行保管。

## 13. 验收标准

### 13.1 移动端

- 390×844 视口下核心页面无横向滚动。
- 双人信息始终同屏、同行对照。
- 月度录入的核心字段无需在两套人物卡之间上下寻找。
- 底部导航不遮挡正文和输入按钮。
- PWA 可添加到手机主屏幕并正常启动。

### 13.2 数据与计算

- 同月覆盖前有二次确认。
- 钱迹 CSV 正确排除收入、退款、不计收支和非目标月份。
- 半年、全年只汇总已有月度记录，并显示覆盖率。
- 生命能量公式与设置页预览、月度结果一致。
- V1 数据升级后已有月份和设置不丢失。

### 13.3 权限与隐私

- 管理者可完整编辑。
- 查看者看到一致数据但不能修改。
- 备份中不存在原始钱迹交易行。
- 未部署安全后端前，不把本地“只读预览”描述为真实访问控制。

## 14. 示例数据说明

设计稿中的 `¥89,100`、`¥8,800`、`¥9,400`、工作天数、通勤时间、工作成本和生命能量结果均为界面示例，不得写死为生产默认值。生产数据来自用户设置、月度录入或演示数据工厂。

