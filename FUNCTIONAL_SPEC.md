# 功能规格

## 1. 产品边界

家庭月度驾驶舱不是记账软件，也不替代钱迹。它每月只记录家庭大数，帮助两个人在十分钟内回答：收入多少、家庭转入多少、花了多少、两张家庭卡还剩多少、储蓄目标是否前进，以及这些支出对应多少生命时间。

## 2. 用户角色

### 管理者：酥梨

可以查看、录入、上传钱迹 CSV、覆盖已有月份、修改设置、导入导出备份。

### 查看者：陈前

可以查看与管理页完全一致的总览、月度记录、生命能量、半年/年终总结和目标进度；不能上传、修改或保存。

本地版的“只读视图”用于验收交互，不构成真实安全控制。正式共享链接必须由阿里云服务端分别校验管理密码和只读密码。

## 3. 页面与导航

底部固定三个入口：`总览`、`月度记录`、`生命能量`。家庭设置从右上角进入。

### 3.1 总览

- 默认显示最新保存月份，可切换到其他月份。
- “查看周期”同时提供月份、上半年、下半年和年终总结。
- 半年与年终总结完全由月度记录自动汇总，不增加重复录入。
- 不完整周期显示“已记录 N/6 个月”或“已记录 N/12 个月”，不冒充完整总结。
- 显示家庭储蓄卡余额、前一条记录以来的变化和目标进度。
- 显示家庭总收入、共同转入、家庭总支出、经营结余。
- 显示家庭花销卡余额、3,000 元默认预算、使用率和预算剩余。
- 分别显示两人的到账工资、家庭转入、当月个人留存、未分配差额。
- 最多给三条建议，优先解释预算、自动转入比例、个人分配差额或家庭卡解释差额。
- 建议保持中性，不羞辱消费，也不把最低支出当成唯一目标。
- 半年与年终总结额外显示平均每月支出、期初到期末储蓄变化、累计生命能量和累计分类结构。

### 3.2 月度记录

管理者录入：

- 自然月。
- 家庭花销卡和家庭储蓄卡当前余额。
- 两人的到账工资、转入家庭、当月个人留存。
- 钱迹自动汇总或人工确认的家庭总支出。
- 200 字以内的大事或异常说明。

规则：

- 所有金额必填且不能为负数。
- 同一月份只有一条快照；覆盖前必须二次确认。
- 保存成功后回到该月总览。
- 只读视图中表单和上传入口不可编辑。

### 3.3 钱迹 CSV 汇总

- 支持 UTF-8、UTF-8 BOM 和 GB18030。
- 识别时间、分类、二级分类、类型、金额，以及可选的不计收支字段。
- 仅统计当前录入月份中 `类型=支出` 的正金额记录。
- 排除退款、不计收支、收入和其他月份。
- 上传后显示文件名、有效支出笔数、支出总额和前三个一级分类。
- 允许管理者修改最终确认值；自动汇总值仍保留。
- 本机与未来云端都只保存汇总值，不保存原始 CSV 或交易明细。

### 3.4 生命能量与趋势

- 展示最近 12 条记录的家庭总收入、共同转入和家庭总支出。
- 展示家庭储蓄卡余额变化和目标进度。
- 展示最近月份的钱迹一级分类结构。
- 分别显示两人的实际时薪和“每赚 10 元所需分钟”。
- 用家庭综合实际时薪换算最近月份的家庭支出生命小时。
- 明确提示生命时间用于判断“值不值得”，不用于制造内疚。

## 4. 设置与数据管理

- 家庭月度花销预算，默认 3,000 元。
- 家庭储蓄目标，默认 100,000 元，可修改。
- 两人昵称。
- 两人的参考税后月薪、工作小时、通勤小时和工作成本。
- 跟随系统、浅色、深色三种主题。
- 导出全部业务数据为 JSON、从 JSON 恢复、二次确认后清空本机数据。
- 切换管理视图和只读预览。

## 5. PWA 与状态

本地 V1：

- 可添加到手机主屏幕。
- 应用外壳可离线打开。
- 数据存放于当前浏览器 `localStorage`。
- 具备空状态、解析错误、保存成功和操作反馈。

阿里云共享阶段：

- 同一 HTTPS 地址提供管理登录和只读登录。
- 管理会话可读写，只读会话只能读取。
- 服务端使用函数计算访问私有 OSS，浏览器不得持有阿里云 AccessKey。
- 云端完成前，不对外发送任何家庭财务数据。

## 6. 验收场景

1. 上传同时含有收入、支出、退款、不计收支和多个自然月的钱迹 CSV，只汇总当前月有效支出。
2. 保存首月后，总览显示“首月记录，暂无环比”。
3. 保存第二个月后，正确计算储蓄卡变化与两张家庭卡解释差额。
4. 家庭支出 3,300 元时，显示超预算 300 元。
5. 两人收入 20,000 元、转入 15,000 元、个人留存 4,000 元时，显示未分配差额 1,000 元。
6. 选择下半年时，正确累计 7 至 12 月已有记录并显示覆盖月份数。
7. 选择年终总结时，正确累计 1 至 12 月已有记录，不要求再次录入。
8. 覆盖已保存月份前出现确认提示。
9. 只读视图下所有月度编辑控件禁用，设置中只保留返回管理视图。
10. 导出备份中不存在钱迹原始行、交易备注或交易对手。
11. 在 390 像素宽手机视口中三页均可阅读和操作。
12. 连续三个月都能在十分钟内完成录入，并清楚回答家庭经济状况。
